# Single-embodiment baseline: bridge alone, trained from scratch (no cross-embodiment modeling).
defaults:
  - model: base
  - data: bridge
  - training: default

model:
  conditioning: ee
  action_dim: 7

training:
  tag: baseline_bridge
  output_dir: model_ckpt/baseline_bridge
